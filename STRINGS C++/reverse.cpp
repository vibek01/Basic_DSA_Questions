#include<iostream>
#include<string>
using namespace std;
int main(){
    string s = "Prasad";
    // int n = str.length();
    cout<<s<<endl;
    // reverse(s.begin(), s.begin()+3);
    // str.append(" Bin");
    cout<<s;
    // getch();
    return 0;
}